<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="user">
	<select id="totalUser" resultType="map">
	  select * from EMPLOYEES	
	</select>
	
	<insert id="insert" parameterType="join.vo.UserVO">
		insert into t_user (idx, id,	pwd,	name,	nickname,	phone, email,
							age,	gender,	addr1,	addr2,	blood,	file_name,
							ori_name,	intro_content,	ip,	status,	reg_date)
		values(#{idx}, #{id},	#{pwd},	#{name},	#{nickname},	#{phone} , #{email},
							#{age},	#{gender},	#{addr1},	#{addr2},	#{blood},	#{file_name},
							#{ori_name},	#{intro_content},	#{ip},	#{status},	#{reg_date})
	</insert>
		
</mapper>  





